// use 'require' keyword to import module
const mymath = require('./mymath.js');

// results in 6
console.log(mymath.adder(4, 2));

// results in 2
console.log(mymath.subtractor(4, 2));

// results in 8
console.log(mymath.multiplier(4, 2));

// results in 2
console.log(mymath.divider(4, 2));